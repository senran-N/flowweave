# 项目地图

## 项目是做什么的

这是一个实验性的多路径传输项目，暂定名为 FlowWeave / 织流。研究方向是：把 Wi-Fi、移动网络或多条宽带连接编织成一条连接。

长期目标同时包含三件事：

- A：网络切换时，正在进行的连接不断开。
- B：多条线路可以共同传输，提高总速度。
- C：丢包严重时，实时数据仍尽量保持低延迟。

目前第二阶段 A 已验证“开放路径同路返回 PATH_ACK、关闭后跨路回退”的反馈面修复，并定位正向种子 1103 的无 PTO 根因。默认关闭的 abandoned 即时对冲已把该种子的最大断流从约 `14.75 s` 降到 `3.71 s`：业务数据不再等待 3 PTO 路径清场。ACK 进展恢复时钟把恢复触发提前到故障后约 `0.8 s`，有界跨路径 ACK 逃生又把主路累计确认从约 `3.41 s` 提前到 `0.87 s`，最终让同一种子最大断流降到 `1006.35 ms`。这比上一轮 `3350.03 ms` 缩短约 2.34 秒，但仍比 `<1000 ms` 门槛多 `6.35 ms`，所以 A 依然失败。最新论文复核把候选收敛成两级恢复：先用最多一包的可靠性探针问清接收状态，现有 ACK 进展 PTO 到期后才搬完整未确认集合；若代表场证明 PATH_ACK 仍不够，再考虑类似 MPTCP Data ACK 的数据级连续 offset 反馈。下一步仍先用同一候选跑正向和反向 1101，不能围绕单次 6 毫秒盲调。第二阶段 B 的容量观测路线仍暂停。项目还不是可日常使用的代理软件，也没有宣称性能超过 Hysteria 2、VLESS 或其他现有方案。

## 当前技术选择

- 核心语言：Rust。
- MPQUIC 实验底座：NoQ 1.0.1 加 FlowWeave 的最小可审计测量和 PTO 恢复补丁；失败调度已全部删除，NoQ 当前保持官方发包顺序。它是实验发动机，不是永久绑定的协议内核。
- 加密：标准 TLS 1.3；实验使用内存中临时生成、被客户端精确校验的证书。
- 普通功能实验：Linux 回环地址，只监听本机，外部设备无法连接。
- 坏网络实验：rootless 隔离网络命名空间加 Linux tc netem。网络限制随脚本退出而消失，不修改真实网卡。
- 依赖策略：没有下载新的第三方包；根项目直接引用已经锁定并本地替换的 `noq-proto` 1.0.1，读取通用传输配置和逐路径测量指标。
- 公平基准：固定对手为 Hysteria 2.9.3；最终结论必须满足 BENCHMARK.md，不能根据跑分临时改规则。

## 当前进度

### 第一阶段：真正的 MPQUIC 双路径

2026-07-11 已完成并实际运行：

- 客户端与服务端成功协商 MPQUIC。
- 同一连接建立两条路径，不是两个互不相干的普通 QUIC 连接。
- 两条路径分别承载了实际数据。
- 主路径被正常关闭后，原连接继续通过第二路径传输。
- QUIC Datagram 能够往返。
- 第三条超限路径、格式错误的数据和超出实验上限的参数会被拒绝。

### 第二阶段 A：可重复坏网络实验场

2026-07-11 已建立并实际运行：

- 两条线路可以分别设置带宽、延迟和丢包。
- 每个对照项开始前都会重置网络队列；丢包模拟使用固定随机种子，减少“不同选手抽到不同随机题目”的偏差。
- 能对比仅线路一、仅线路二和 NoQ 默认多路径。
- 能记录断路恢复时间、流吞吐量、Datagram P50/P95/P99、应用层丢失、额外流量和每条路径的统计。
- 测试只允许在一次性隔离网络空间运行；即使手动伪造启动标记，只要仍在主网络空间就会拒绝修改队列。

连续三轮固定场景的结果：

- 20 ms / 0.1% 与 80 ms / 1% 两路同时可用时，默认多路径吞吐量约 10.15～10.45 Mbit/s，两条路径都显著承载了流量。
- 实验把单路径空闲判定上限设为 3 秒；主线路在传输中变为 100% 丢包后，原连接约 3.99～4.40 秒恢复，数据改走备用线路。
- 8% 与 2% 高丢包场景中，默认多路径吞吐量三轮约为 3.82、2.40 和 8.10 Mbit/s；相对当轮最佳单路径，两轮更快，一轮只剩约 46%。默认多路径的 Datagram 应用层丢失约为 13.89%～18.06%。
- 8 Mbit/s 低延迟线路与 25 Mbit/s 高延迟线路组合时，默认多路径相对当轮最佳单路径约在 -3.2%～+1.9% 之间，没有稳定聚合收益。

这些数字是实验基线，不是产品性能承诺。固定随机种子能减少丢包题目差异，但不能消除 QUIC 拥塞窗口、重传时机、操作系统调度和短测试数据量造成的波动。高丢包场景的三轮结果得出了不同的吞吐量排名，因此后续任何算法比较都必须看多轮中位数和波动范围，禁止挑选最好看的一次。

### 第二阶段 A：PTO 跨路径对冲候选

2026-07-12 已完成并实际运行：

- 最新 MPQUIC 草案允许逐路径 PTO 后在其他可用路径重传延迟包内容；SingleR 和 MRC 进一步支持“一个时刻补一次”以及“快速恢复数据、保守判断路径”的分离设计。
- NoQ 增加默认关闭的通用配置。官方基线与候选使用同一版本、同一 TLS 1.3、同一网络种子；A 组恢复机制没有混入 B 组调度枚举。
- 重复、重叠、乱序 ACK 只扣一次未确认字节；任一副本被确认后，尚未发送的无用重复范围会立即取消。
- 触发对冲时记录本路径下一包号；黑洞前已经在路上的旧 ACK 不能错误恢复主路，只有对冲后发出的探针被确认才恢复普通数据调度。
- 确定性测试覆盖默认关闭、单路径不触发、首次和重复 PTO、备用路拥塞窗口、恢复、纯 FIN、两种副本 ACK 顺序和旧 ACK 边界。
- 五种子原始结果保存在 `benchmark-results/2026-07-12-pto-hedge-screening.csv`。

短筛结果：

- 黑洞首字节恢复 5/5 低于 1 秒，P95（五样本即最慢值）为 393.00 ms；NoQ 默认基线约为 3.47～3.61 秒。
- 完整 256 KiB 候选校验仍需约 1.13～3.63 秒，因为 80 ms 冷备用路需要慢启动；该数字与“断流多久”分开报告，不能混用。
- 正常网络候选吞吐相对基线中位 99.45%，UDP 总量中位 100.68%，两条路径 5/5 保持开放。
- 高丢包候选 UDP 总量相对基线中位 100.40%，5/5 数据正确并至少保留一条路径。
- 候选五轮共触发 9 次对冲，新加入重传队列的 STREAM 载荷 413,248 字节。

候选因此通过了预先锁定的三组短筛并进入正式 A 组；后续持续流结果证明，短筛只看“故障后第一个有效字节”会漏掉后续长停顿，不能继续作为晋级结论。

### 第二阶段 A：30 秒双向正式实验

2026-07-12 已完成 release 构建下的 20 场正式实验：同一条持久 QUIC 流持续发送 30 秒，第 10 秒把主路改为 100% 丢包，正向和反向各使用 5 对固定种子，并交替运行 NoQ 基线与 PTO 候选。接收端逐条校验序号、载荷和摘要，断流指标取故障后最大的相邻正确记录到达间隔。

完整原始结果保存在 `benchmark-results/2026-07-12-pto-hedge-formal-a-v2.csv`：

- 正向候选 5/5 数据完整，但断流中位 `5570.67 ms`、P95/最差 `10830.88 ms`，远高于 `<1000 ms` 门槛；NoQ 基线中位 `6156.74 ms`、最差 `8963.60 ms`。
- 正向 5 轮只有 3 轮在故障后触发对冲，共重新入队 `1,789,216` 字节；其余 2 轮完全没有对冲。即使触发，对冲后的断流仍为 `2.80～5.57 s`，说明“一次搬运当前仍在途的 STREAM 帧”不能覆盖持续流的全部阻塞状态。
- 正向候选故障后 UDP 相对同轮基线中位 `101.18%`，额外流量不是本次失败的主要矛盾。
- 反向候选与 NoQ 基线均为 `0/5` 完整。发送端在主路黑洞后没有从健康备用路收到足够反馈，最终等待记录载荷超时；这暴露了数据能换路但 PATH_ACK/ACK 可能仍经故障主路返回的反馈面缺口。
- 反向候选还在故障前误触发过 1 次、4 字节的对冲，证明正常期误判也必须继续计账。

`benchmark-results/2026-07-12-pto-hedge-formal-a.csv` 是第一次诊断运行的 5 条不完整记录；当时 12 秒收尾上限会误杀仍在恢复的场次。它保留作实验器缺陷证据，不与修正后的完整 v2 数据混算。

正式结果否决了当前候选，但实验场本身有效：它同时抓到了“有对冲仍长停顿”“完全不对冲”和“反向反馈链路失效”三类短筛看不到的问题。下一轮不得调低门槛或只重跑好看的种子，必须先增加事件时间线并分别修复数据面与反馈面。

随后增加了不改变算法的三场时间线诊断，原始数据保存在 `benchmark-results/2026-07-12-pto-hedge-diagnostic.csv`：

- 正向种子 1101 在故障后约 `304 ms` 已完成 PTO 与对冲，但主路约 `4009 ms` 才关闭，备用路约 `4519 ms` 才第一次从本路径发回 PATH_ACK，完整记录直到约 `5348 ms` 才恢复。长停顿主要是反馈绕回黑洞主路，不是第一次 PTO 太慢。
- 正向种子 1103 没有触发任何主路 PTO，直接等到约 `5488 ms` 关闭主路，最大断流约 `10.86 s`。这是 ACK 修复后要单独复查的第二个问题。
- 反向种子 1101 在故障发生时备用路已经关闭。此前它发送过约 `1.19 MiB` 新数据，但确认被主路带回，发送端把这条健康但“只出不进”的路误判为空闲。
- NoQ 确定性测试已复现 `PATH_ACK[path 1]` 实际从 path 0 发出的行为。诊断统计现在能区分 PATH_ACK 确认的路径与实际发送路径，并记录首次 PTO、对冲、同路 ACK、路径关闭和最大断流窗口的毫秒时间线。

架构结论：多路径不是“数据能从两条路发出”就完成了；拥塞窗口、PTO 和路径存活都依赖 ACK 反馈。第一修复优先级必须是反馈面路径韧性，暂不增加 FEC、窗口预留或新的失效检测器。

### 第二阶段 A：PATH_ACK 同路反馈修复

2026-07-12 已实现并实际验证：

- 开放路径优先在本路径发送确认该路径的 PATH_ACK；Backup 路径可以发送 ACK-only 包，不再因应用数据调度偏好而失去反馈。
- 只有被确认路径已经 abandoned 或没有可用远端 CID 时，才从另一条开放路径跨路回退；关闭路径会把已有延迟 ACK 立即转为可发送，abandoned 路径不会继续发送自己的 ACK。
- 两项确定性测试分别证明“开放 Backup 同路确认”和“路径关闭后跨路回退”；NoQ 396 项测试、FlowWeave 8 项库测试以及两边 Clippy 均通过。
- 三场修复后原始数据保存在 `benchmark-results/2026-07-12-path-ack-affinity-diagnostic.csv`，旧诊断文件没有覆盖。

修复后代表结果：

- 正向种子 1101：备用路首次同路 PATH_ACK 约 `59.90 ms`，不再等待约 4.5 秒；但最大断流仍为 `5872.79 ms`。主路约 `477.50 ms` 已 PTO 并对冲 `664,037` 字节，说明剩余长停顿主要不是反馈黑洞，而是最早有序数据缺口没有被一次对冲完整清除。
- 正向种子 1103：备用路约 `204.82 ms` 开始发包、约 `312.05 ms` 已同路确认，但主路故障后仍完全没有 PTO，直到约 `14905.58 ms` 才关闭；最大断流 `14739.06 ms`。这稳定复现了独立的检测缺口。
- 反向种子 1101：故障时两条路径都保持开放，备用路约 `6.21 ms` 开始发包、约 `12.08 ms` 同路确认，传输由修复前失败变为数据完整；最大断流降为 `1349.29 ms`，但仍略高于 `<1 s` 门槛。

这项修复解决了协议正确性问题，但没有让 A 达标。反馈等待 `h_feedback` 已不再是主导项；剩余工作严格分成“无 PTO 的健康检测”和“最早有序缺失范围优先恢复”，不得混在一次参数调整里。

### 第二阶段 A：无 PTO 状态机根因

2026-07-12 已用正向种子 1103 单独复现，原始数据保存在 `benchmark-results/2026-07-12-no-pto-state-diagnostic.csv`。这轮只增加只读状态，未改变调度、PTO 或路径空闲上限；NoQ 397 项测试、FlowWeave 8 项库测试以及两边 Clippy 均通过。

- 故障瞬间主路并非“没有在途包”：有 `794` 个需确认包、约 `1.15 MiB` 在途字节，PTO 定时器已武装；故障后最高达到 `1305` 个需确认在途包、约 `1.87 MiB`。
- 主路故障时 RTT 约 `465.95 ms`、PTO 约 `540.14 ms`。它仍零星发送需确认包直到 `3216.63 ms`，所以按“最后一次需确认发包 + PTO”计算的截止至少在 `3756.77 ms`。
- 主路最后一次收到 UDP 是 `248.56 ms`；3 秒路径空闲约在 `3250.14 ms` 先触发，使路径进入 abandoned 并停止普通 PTO。普通时限丢包计时器为 0，包表没有提前被清空。
- MPQUIC 草案要求收到 PATH_ABANDON 后继续保留路径编号空间约 3 PTO，以处理乱序包和防止误发 Stateless Reset。该轮结束时主路 PTO 已升到约 `3825.13 ms`，3 PTO 约 `11.48 s`；路径状态在 `14888.37 ms` 才删除，和观测到的约 `11.64 s` 清场间隔一致。
- 备用路在 `6.67 ms` 已发包、`115.09 ms` 已同路确认，但最早阻塞记录直到 `14985.51 ms` 才恢复。最大断流 `14747.71 ms`，说明数据被卡在“路径已 abandoned、但还没最终判丢”的保留期，而不是备用路不可用。

架构结论：标准要求保留路径状态，不等于必须把业务恢复也拖到 3 PTO 之后。下一候选要分开处理：路径编号空间继续按标准保留；未确认 STREAM 数据在进入 abandoned 时立即做一次去重安全的跨路对冲；更早的黑洞检测则使用以 ACK 进展为锚点、不会被后续发包反复推迟的独立恢复时钟。禁止直接缩短 3 PTO 保留期或 3 秒路径空闲上限来掩盖问题。

该候选已完成确定性实现和真实单场验证，但仍未达标：

- 新开关默认关闭，PTO 对冲与 abandoned 即时对冲可以分别复现；历史诊断仍使用 PTO-only 配置。
- 启用后，进入 abandoned 会把当前路径包表中的 STREAM 范围加入全局重传队列一次；原包号状态和晚到 ACK 处理保持到正常 3 PTO 清场。
- 关闭开关时不会改变上游行为；启用时仍要求存在另一条已验证路径，并继续遵守目标路径拥塞窗口和 pacing。
- 确定性测试证明数据在路径状态删除前已经从 Backup 送达，同时原路径包表仍保留；NoQ 399 项测试、FlowWeave 8 项库测试以及两边 Clippy 均通过。
- 真实正向种子 1103 的原始数据保存在 `benchmark-results/2026-07-12-abandon-reinjection-diagnostic.csv`：路径进入 abandoned 时一次对冲 `1,224,458` 字节，最大断流从上一轮 `14747.71 ms` 降到 `3712.71 ms`。
- 主路仍到 `15291.20 ms` 才最终删除，但最大断流已在 `3878.45 ms` 结束，直接证明业务恢复和 3 PTO 路径状态保留已经解耦。
- 剩余断流起于故障后约 `165.73 ms`、止于 `3878.45 ms`；LossDetection 在约 `3388.26 ms` 停止后约 490 ms 恢复，说明现在的主导项就是 3 秒 PathIdle，而不是路径清场。

### 第二阶段 A：ACK 进展恢复时钟候选

2026-07-12 已完成默认关闭的确定性实现和正向种子 1103 真实诊断：

- 标准 QUIC PTO 与 LossDetection 完全保留；新增时钟只在当前路径仍压着 STREAM 数据、且存在另一条已验证路径时工作。
- 纪元从首个在途 STREAM 包开始，真正新确认的 ack-eliciting 包会重置；继续发新包不会移动截止。触发预算仍使用当前路径的 QUIC Data PTO，不引入固定 RTT 魔数或在线概率模型。
- 每个无 ACK 进展纪元最多对冲一次，并在原路径发送一个带立即确认请求的 PING。原路径只暂停普通数据，不改变公开 PATH_STATUS；只有恢复边界之后的探针 ACK 才恢复普通调度。
- 逐路径在途 STREAM 计数为 O(1)，没有在每次 ACK 时扫描整张包表；PTO、abandoned 与 ACK 进展三类统计完全分账。
- 确定性反例已经证明：半个 PTO 后继续发送新数据会推迟标准 PTO，但不会推迟 ACK 进展截止；到原截止时 ACK 进展对冲已经发生，而标准 `pto_timeouts` 仍为 0。
- NoQ 全量 410 项测试、FlowWeave 全量 9 项普通测试及两边 Clippy 已通过。
- 原始结果保存在 `benchmark-results/2026-07-12-ack-progress-reinjection-diagnostic.csv`，SHA-256 为 `acc908ddf6560b0ce440eaa79798ef368028aa43f5145b13a00abca622bcd917`；表头和数据行均为 109 列。
- ACK 进展超时和对冲都在故障后 `773.733 ms` 发生，主路标准 PTO、PTO 恢复和 abandoned 恢复均为 0；该轮只触发 1 次，重新排队 `1,117,749` 字节，证明新时钟确实绕过了滑动 PTO。
- 最大断流仍为 `3545.120 ms`，只比 abandoned 即时恢复的 `3712.711 ms` 缩短约 `167.59 ms`。断流在 `241.577 ms` 开始，到 `3786.698 ms` 才由记录 1452 结束，因此 A 仍明确失败。
- 故障前主路首次数据为 `23,815,667` 字节；按每条记录 `16,400` 字节计算，只进入记录 1452 约 `2,867` 字节。发送端却必须面对约 `1.12 MiB` 未确认集合，说明“ACK 丢失但数据已到达”和“数据真正缺失”混在一起，恢复总字节数不能代表最早缺口已被服务。
- 只读复跑保存在 `benchmark-results/2026-07-12-ack-progress-service-timeline-diagnostic.csv`：备用路在 `868.854 ms` 与 ACK 进展对冲同刻开始重传，但直到 `3405.549 ms` 才首次代其他路径返回 PATH_ACK；最大断流在 `3649.614 ms` 结束，二者只差约 `244.07 ms`。备用路首次新数据要到 `8822.129 ms`，说明恢复记录来自重传队列，而非新数据绕过队列。
- 该时间线把因果闭环补齐：早触发的数据恢复没有及时缩小“未确认集合”，真正解锁队列的是 3 秒 PathIdle 附近才出现的跨路径累计确认。下一候选将用现有 `IMMEDIATE_ACK + PATH_ACK` 组合做一次有界反馈逃生，不发明新的 offset 提示帧。

### 第二阶段 A：有界跨路径 ACK 逃生候选

2026-07-12 已完成默认关闭的确定性实现和正向种子 1103 真实诊断：

- 保留旧 `CrossPathRecovery` 变体不变，新增独立实验变体；历史 CSV 仍能复现，不把新机制偷偷混入旧候选。
- PTO 或 ACK 进展恢复成功后，只在实际承载恢复数据的已验证备用路放置一次 `IMMEDIATE_ACK` 请求；接收端必须在同一个包里同时看到 STREAM 数据，才允许把其他路径尚保留的累计 PATH_ACK 放进这条备用路的立即响应。
- 普通 `IMMEDIATE_ACK`、MTU 探测和路径验证不会武装跨路逃生；正常开放路径仍优先同路 ACK。被选备用路失效时会清理逃生偏好，原 ACK 范围不丢失。
- 没有修改公开 PATH_STATUS、3 秒 PathIdle、标准 LossDetection/PTO 或 3 PTO 路径清场，也没有增加新帧；请求和实际逃生 PATH_ACK 分别计数。
- 实现过程中确定性测试抓到并修复了一次“主路仍声称有 ACK 可发、构包时却被抑制”导致的空包发送循环；修正后只有被选备用路宣布可发送。
- NoQ 全量 413 项测试、FlowWeave 10 项普通测试及两边 Clippy 已通过。
- 原始结果保存在 `benchmark-results/2026-07-12-cross-path-ack-escape-diagnostic.csv`，SHA-256 为 `c74826b8f13f067bc2c3b40e54e0700b53ea57c735a7a9db5afaf811704c2de3`；表头和数据行均为 116 列。
- ACK 进展恢复在 `783.550 ms` 触发；发送端备用路请求 1 次，接收端备用路实际返回 1 个逃生 PATH_ACK。累计确认在 `865.403 ms` 到来，比上一只读轮的 `3405.549 ms` 提前约 `2540.15 ms`。
- 最大断流从 `161.115 ms` 持续到 `1167.468 ms`，共 `1006.353 ms`；相对上一轮 `3350.027 ms` 缩短约 `2343.67 ms`，但仍超门槛 `6.353 ms`，不能算通过。
- 标准 PTO、PTO 对冲和 abandoned 对冲仍为 0，数据完整；备用路首次新数据到 `7469.798 ms`，说明本次快速恢复仍来自重传集合被累计 ACK 剪枝，而非绕开旧队列。
- 下一步先跑正向/反向 1101 代表场。只有不存在更大结构性问题时，才讨论如何给 1103 留出稳定余量；不能拿一次接近门槛的结果直接晋级。

### 第二阶段 B：调度与公平基准

2026-07-11 已锁定公平验收合同：

- A、B、C 都有固定网络条件、测试时长和数字门槛。
- 每个场景至少运行 5 轮并保留全部原始结果。
- Hysteria 2.9.3 的 BBR 和 Brutal 两种模式都必须测试。
- 远程服务器只证明真实部署可用，不能单独证明两条物理线路的带宽聚合。
- NoQ Proto 1.0.1 已原样引入并单独提交，随后增加了可撤销的最小调度补丁。
- FlowWeave 当前只对外保留 NoQ 默认基线；轮询、最低 RTT、预计最早送达和交付速率加权都已按数据删除，不留失败算法开关。
- NoQ 当前恢复官方发送路径；下一代候选必须先有论文依据和确定性测试，短筛通过后才允许长期保留。
- NoQ 路径统计已把“首次发送的 STREAM 数据”和“重传数据”分开，避免用重复流量冒充两路有效聚合。
- 首次数据统计有确定性测试；本次清场后 NoQ 380 项单元测试、3 项文档测试，FlowWeave 4 项普通测试和 1 项网络实验防伪回归均通过，两边 Clippy 零警告。

2026-07-11 的隔离坏网络单轮初筛结果：

- A 换网：四种策略都在原连接恢复，但仍约需 4.02～4.49 秒。根因仍是 3 秒路径空闲判定，不是数据调度顺序。
- B 异构线路：最佳单路为 8.38 Mbit/s；NoQ 默认 7.79、轮询 8.59、最低 RTT 8.45、预计最早送达 10.92 Mbit/s。预计最早送达本轮约高 30%，值得进入五种子复测，其余策略尚无稳定聚合证据。
- C 高丢包：预计最早送达本轮流吞吐量最高，为 7.19 Mbit/s；但四种多路径策略的 Datagram 丢失仍约 9.72%～15.28%，明显不合格，不能靠调度冒充冗余或 FEC。

这些都是单轮初筛数据，不是最终性能结论，因此随后按固定 5 个种子进行了候选筛选；失败候选必须删除，不能永久堆成开关。

随后完成的 2 MiB 五种子候选筛选，原始 60 轮保存在 `benchmark-results/2026-07-11-scheduler-screening.csv`：

- 平衡 15+15 Mbit/s：默认、轮询、最低 RTT、预计送达四种策略都是 5/5 轮比最佳单路快至少 15%，每条路首次数据占比均远高于 10%。
- 异构 8+25 Mbit/s：默认 0/5、轮询 3/5、最低 RTT 1/5、预计送达 3/5 达到“快 15%”。没有策略通过 4/5 阶段门槛。
- 最低 RTT 的证据最弱，已从 NoQ 补丁、FlowWeave 接口和实验矩阵完整删除。
- 轮询的异构中位数约为最佳单路的 122.2%，预计送达约为 116.2%；但两者最差轮分别只有 95.6% 和 91.9%，只能进入 20 秒/64 MiB 长时复赛，不能宣称稳定聚合。
- 所有多路径候选每轮较少线路都承载至少约 35.8% 的首次应用数据；这证明问题不在“第二条路没用上”，而在短流、拥塞窗口和异构线路调度效率。

随后按正式时长完成 release 长时复赛：每场单连接预热 2 秒，再计时至少 20 秒；50 场原始结果保存在 `benchmark-results/2026-07-11-scheduler-long.csv`：

- 平衡 15+15 Mbit/s：轮询 5/5 轮达到“比最佳单路快 15%”，吞吐量中位 18.76 Mbit/s，相对最佳单路中位 152.4%，每轮较少线路首次数据占比至少 30.4%。
- 平衡线路中的 NoQ 默认和预计送达都长期偏向一条路：相对最佳单路分别只有约 102.4% 和 105.3% 中位数。
- 异构 8+25 Mbit/s：轮询仅 1/5 轮过线，中位相对最佳单路 100.1%，最差 86.6%；预计送达 0/5 轮过线，中位 95.8%，最差 86.7%。
- 轮询的问题是按批次平均分配，无法按 8/25 Mbit/s 的容量比例分配，慢路会造成乱序和队头等待；预计送达的问题是形成路径偏置，缺乏持续探索，常退化成最佳单路甚至更慢。
- 两个候选都没有同时通过平衡和异构场景，必须删除。下一代候选应按每条路径的实际交付速率加权，并保证低频探索，不能继续微调这两个失败公式。
- 长测记录了 CPU、RSS、首次数据、重传和线速。CPU 可逐场比较；RSS 因同一进程的分配器会保留历史内存，目前只用于发现异常增长，最终跨程序内存比较必须隔离进程。

随后实验“交付速率加权”：用 `拥塞窗口 / 平滑 RTT` 作为权重，平滑按比例发包，并给慢路保留至少约 10% 探索机会。修复了路径建立控制包污染调度积分的问题后，全部确定性测试通过，再运行 40 场五种子短筛；原始结果保存在 `benchmark-results/2026-07-11-delivery-rate-weighted-screening.csv`：

- 平衡 15+15 Mbit/s：5/5 轮达到“比最佳单路快 15%”，吞吐量中位 20.65 Mbit/s，相对最佳单路中位 171.6%。
- 异构 8+25 Mbit/s：0/5 轮过线，吞吐量中位 12.06 Mbit/s，相对最佳单路中位 96.2%，最差 83.6%。
- 两条线路首次数据仍接近各占一半，证明 `拥塞窗口 / RTT` 没有学到 8/25 的真实容量比例；UDP 线速中位达到应用数据的 1.09 倍，额外重传也没有换来收益。
- 该候选没有通过短筛，因此没有浪费时间运行长测，并已从 FlowWeave 和 NoQ 分叉完整删除。
- 下一代候选不再使用瞬时拥塞窗口冒充线路带宽；将先调查 MPQUIC/MPTCP 论文，再结合实际 ACK 交付率、预计到达时间和队头阻塞代价建立可解释模型。

随后按论文模型实现 ACK-ECF：只用真实应用数据 ACK 学习逐路径交付率，再按 `最小 RTT/2 + (在途字节 + 新包字节)/确认交付率` 预测完成时间；新路径有最多 8 个正常应用包的有偿探测，并加入 ECF 式等待。数学、真实 ACK 接线、控制包隔离、单路径和 Backup 等测试全部通过后，运行了 40 场固定五种子短筛，原始数据保存在 `benchmark-results/2026-07-11-ack-ecf-screening.csv`：

- 平衡 15+15 Mbit/s：5/5 轮过线，中位 19.68 Mbit/s，相对最佳单路中位 161.2%；但 CPU 中位 43.3%，明显高于 NoQ 默认的 25.9%。
- 异构 8+25 Mbit/s：0/5 轮过线，中位 11.80 Mbit/s，相对最佳单路中位 95.5%，最差 74.7%。较慢的 8 Mbit/s 线路仍承担中位约 44.0% 的首次数据，远高于按容量比例应有的约 24.2%。
- 根因是一个反馈闭环：普通 ACK 速率只能说明“调度器刚才给这条路喂了多少”，不能说明“线路最多还能吃多少”。初始接近 1:1 的投喂会产生接近 1:1 的 ACK 速率，再反过来证明 1:1，看似在线学习，实际在学习自己的决定。
- 这说明完成时间公式在已知真实容量时可以成立，但容量输入不可直接从未饱和、被调度器限流的 ACK 流中得到。确定性测试把真实 8:25 速率作为已知输入，因此不能替代这一可观测性检查。
- ACK-ECF 没有通过短筛，未运行长测，代码、开关和筛选参赛项已完整删除。下一路线必须只在路径被明确饱和或受控探测时估容量，并区分“应用受限”“调度器受限”和“线路受限”；优先调查 packet-pair / packet-train、pathChirp 与 BBR 式 delivery sampler，而不是继续平滑同一份循环反馈。

随后独立实现并真实筛选 packet train 与 Chirp 排队曲线拟合容量传感器。两个候选每次测两条路各约占 2 MiB 短流的 1.87%，先后使用用户态时间戳、Linux 内核接收时间戳，以及把限速与 netem 延迟/丢包拆开的实验场。四轮诊断最终仍未过线；最后一轮原始数据保存在 `benchmark-results/2026-07-11-capacity-probe-screening.csv`：

- Packet Train 只有 4/10 个读数通过自身质量门槛，快慢排名 1/5，容量比例误差门槛 1/5。
- Chirp 有 9/10 个读数通过自身质量门槛，快慢排名 4/5，但容量比例误差门槛仅 1/5，中位比例误差 49.62%。
- 多个严重错误的 Chirp 读数仍有很低的归一化拟合误差，证明“曲线拟合得好”不等于路径真的服从该流体队列模型；当前置信度会被模型失配欺骗。
- 本地回环上的短探针强烈受限速器令牌初态、定时器和多级队列影响，不能认证真实 WAN 可用容量。
- 同一路线已超过三次失败，现按规则停止。失败实现在实验快照 `ff0f9d3` 后已完整删除，不接 NoQ；完整论文推导、四轮诊断和下一步选择见 `CAPACITY_PROBE_RESEARCH.md`。

2026-07-12 又验证了推荐的 BBR3 只读容量路线：通过通用接口读取每条路径的 `max_bw`、最新交付率、应用受限状态、主动探测状态与 filled-pipe 资格；Cubic/NewReno 明确不提供容量模型。固定五种子仍使用 2 MiB 正常应用数据，不额外预热，原始数据保存在 `benchmark-results/2026-07-12-bbr-capacity-screening.csv`：

- 单路 8 Mbit/s 只有 1/5 次在短流结束前取得 filled-pipe 资格；25 Mbit/s 为 0/5。
- 同一 MPQUIC 连接中，两条路径同时取得资格为 0/5；默认调度有一轮完全没有给第二条路首次数据。
- 未合格的原始 `max_bw` 已能粗略区分快慢，但仍处于 Startup。删除资格门槛会再次把“下界或暂时观测”冒充真实容量，因此禁止使用。
- 短筛失败后没有运行免费长预热；只读接口已在实验快照 `29f0ec2` 后完整清场。完整推导见 `CAPACITY_PROBE_RESEARCH.md`。

### 2026-07-12：数学路线审计

继续复核 CloudBurst、FlEC、QUIRL、AC-RLNC、编码多路径队列、Whack-a-Mole、Queueing Bandits、LSQ 和 Coupled BBR 等工作后，方向进一步收敛：

- 普通 ACK 只能看到 `min(投喂量, 真实容量)`；路径未被填满时，真实容量在数学上不可辨认。下一代 B 可以不再输出容量数字，而研究“某路径确认多少新数据，就归还多少本路径新数据信用”的有界完成驱动。
- B 的异构门槛要求 `容量利用率 × 编码有效率 >= 87.12%`；即使每 16 份原数据只加 1 份校验，也要求两路总容量利用率至少 92.57%。因此重型 FEC 不可能替代正确的 B 聚合。
- C 的两路独立双发理论丢失率为 `8% × 2% = 0.16%`，可达到 99.84%；真正风险是 200 字节小包的包头。四条消息一批可把估算线速压到约 2.18 倍，并只增加最多约 30 ms 凑批等待。
- `(2,3)` XOR 虽更省流量，但离 99.5% 门槛太近，不作为第一候选。第一候选仍是可审计的批量双发；只有实测过不了流量门槛才升级真正编码。
- FALCON 需要 729 个预训练粗粒度模型；GPR-MPQUIC 主要依赖神经网络和仿真；QUIRL 的 Tetrys 实现还存在知识产权公开限制。当前都不进入实现。

完整公式、论文适用边界和下一步实验顺序见 `MATH_RESEARCH.md`。

## 这些结果说明什么

- NoQ 默认行为确实能让两条 Available 路径同时发送数据。
- “两路都有流量”不等于“带宽聚合”：异构线路中几乎没有超过最佳单路。
- 单次吞吐量变好不等于算法已经变好：高丢包排名会波动，而且 Datagram 丢失仍然严重。
- A 的 PATH_ACK 同路反馈修复已经让反向代表场从失败变为完整恢复；abandoned 即时对冲又把正向 1103 从 14.75 秒降到 3.71 秒并消除 3 PTO 清场等待。但它仍高于 1 秒，因此 A 仍未通过。
- B 已经淘汰五种自定义调度；当前没有通过筛选的候选，也没有稳定带宽聚合结论。
- A 的恢复机制与 B 的容量调度必须保持分离；旧 ACK、纯 FIN、拥塞窗口和额外 UDP 都已经证明是不可省略的边界。
- C 仍需要关键 Datagram 双发或 FEC；普通路径调度无法把 10% 左右应用层丢失变成合格结果。

## 主要文件

- Cargo.toml：项目名称和经过核实、锁定版本的依赖。
- Cargo.lock：由 Cargo 生成，锁定所有间接依赖的准确版本。
- BENCHMARK.md：提前锁定怎样才算公平、稳定地优于 Hysteria 2。
- SCHEDULER_RESEARCH.md：记录 MPQUIC/MPTCP 调度论文、关键公式，以及 ACK-ECF 为什么在“真实容量不可直接观测”这一点上失败。
- CAPACITY_PROBE_RESEARCH.md：记录 packet train、pathChirp、Voyager、PathKatana、QUIC Receive Timestamps 与 BBR 的容量测量推导，以及独立传感器为何在真实五种子实验中失败。
- FAILOVER_RESEARCH.md：记录 PTO 对冲的数学模型、先进论文复核、状态机、安全边界、短筛与正式持续流失败结论。
- MATH_RESEARCH.md：记录容量不可辨认证明、编码与队列论文审计，以及 A 保留、B 改 ACK 信用、C 先批量双发的数学依据。
- src/main.rs：普通实验的启动入口和中文结果展示入口。
- src/lib.rs：可复用的 MPQUIC 连接、传输校验、指标采集和断路实验逻辑；A 组时间线会同时记录 RTT/PTO、在途包、包表和定时器状态，NoQ 相关操作集中在这里。
- src/scheduler.rs：FlowWeave 对外展示的调度名称；当前只有 NoQ 默认基线，失败候选不保留。
- benchmark-results：真实运行产生的逐轮原始数据和说明；失败轮次不删除。
- tests/network_lab.rs：四类坏网络场景、A 组恢复短筛、正式实验、PATH_ACK 与无 PTO 独立诊断、B 组单路径对照和安全护栏。
- scripts/run_netem_lab.sh：进入一次性隔离网络空间，启动 smoke、A 组 failover、B 组 screen 或 release 长时实验。
- third_party/README.md：记录第三方源码的版本、校验值、许可证和我们实际修改过什么。
- third_party/noq-proto：NoQ Proto 1.0.1 官方源码快照，加 12 个文件组成的最小测量、PTO/abandoned/ACK 进展恢复与 PATH_ACK 反馈补丁；当前没有自定义调度补丁。
- .gitignore：避免把编译产生的临时文件提交进 Git。

## 架构警戒线

后续每增加一层复杂度，必须先回答：

1. 它解决了哪个可以测量的问题？
2. 有没有更简单、已有标准的能力可以复用？
3. 是否让我们的协议错误地绑死在 NoQ 内部？
4. 正常、断路、乱序和恶意输入时分别会怎样？
5. 失败后能否回到最近一次可用提交？
6. 是否在没有基准数据时提前优化？
7. 主人能否用一句人话理解这次变化？

当前硬性边界：

- 不自创加密，继续使用标准 TLS 1.3。
- 没有真实调度收益，不宣称带宽聚合。
- 不扩大 NoQ fork；当前偏离严格记录在 `third_party/README.md`，升级时可逐文件撤销重放。
- B 的轮询、MinRTT、预计送达、交付速率加权和 ACK-ECF 都已按原始数据删除；下一候选必须先有论文依据，再经过同一五种子门槛。
- 下一条 B 路线必须二选一：要么拿出路径确实被填满的证据后再使用容量；要么像有界 ACK 信用一样彻底避开容量数字。禁止继续把调度器自己制造的 ACK 速率当成线路上限。
- A 的 PATH_ACK 反馈根因、无 PTO 检测缺口和旧范围服务阻塞都已定位；有界跨路径 ACK 逃生把 1103 压到 `1006.353 ms`，仍未过线。下一步只跑正向/反向 1101 代表场，禁止缩短路径空闲或 3 PTO 保留期，也禁止围绕单次 6 毫秒差值盲调。
- C 先验证关键 Datagram 双发，再决定是否引入 FEC；不能因为流吞吐量变好就忽略实时包仍大量丢失。

## 当前明确没有做

- 没有代理入口、TUN、SOCKS5 或用户界面。
- 没有真正完成带宽聚合。
- 没有选出最终调度算法；当前没有通过筛选的自定义候选。
- 已完成第一版 30 秒双向正式 A 组和 PATH_ACK 修复后三场诊断；协议正确性有所改善，但 A 仍未通过，也没有与 Hysteria 2 完成 A 组最终对比。
- 没有关键数据双发或 FEC。
- 没有修改真实网络、部署服务器、上传文件或接触真实用户数据。

## 本机验收命令

普通功能实验：

~~~bash
cargo run
cargo test --all-targets
~~~

隔离坏网络实验：

~~~bash
./scripts/run_netem_lab.sh
./scripts/run_netem_lab.sh failover
./scripts/run_netem_lab.sh formal-a
./scripts/run_netem_lab.sh diagnose-a
./scripts/run_netem_lab.sh diagnose-no-pto
./scripts/run_netem_lab.sh diagnose-abandon
./scripts/run_netem_lab.sh diagnose-ack-escape-representative
./scripts/run_netem_lab.sh screen
./scripts/run_netem_lab.sh long
~~~

默认 `smoke` 坏网络实验会运行约一分钟；`failover` 运行 A 组 30 场五种子短筛；`formal-a` 使用 release 构建运行 20 场、约 11 分钟的双向持续换网正式实验；`diagnose-a` 使用三个代表场景运行约 2 分钟的 A 组事件时间线；`diagnose-no-pto` 只复跑正向种子 1103 的 PTO-only 历史状态；`diagnose-abandon` 用相同种子验证 abandoned 即时对冲；`diagnose-ack-escape-representative` 用当前 ACK 逃生候选复跑正向和反向种子 1101，不修改算法或门槛；这些诊断都会记录 RTT/PTO、在途包、包表、最后一次需确认发包和定时器状态；`screen` 运行 B 组五种子短流筛选；`long` 使用 release 构建，运行约 19 分钟的正式时长 B 组复赛。看到最终 `test result: ok` 表示实验基础设施成功完成；指标不好不代表测试程序失败，它可能正是在如实揭示协议短板。
